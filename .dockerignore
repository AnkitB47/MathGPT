# ── Don’t send EB configs into Docker
.elasticbeanstalk/

# ── Git metadata
.git/
.gitignore

# ── Local secrets & keys
.env
key.json

# ── Python / project caches
__pycache__/
*.pyc
*.log

# ── Virtual envs & tooling
qlora-env/
venv/
.venv/
.vscode/

# ── HF / LangChain caches
hf_cache/
transformers_cache/
cache/
datasets/

# ── Large outputs
output/
GPU_codingassistant/

# ── Terraform infra (we track it in Git, but we don’t need it in the image)
terraform/

# ── Docker tooling files
.dockerignore
Dockerfile*
docker-compose*.yml
.docker/

# ── Frontend / Node
node_modules/
dist/
build/
