# ── Secrets & local configs
.env
key.json

# ── Python caches
__pycache__/
*.pyc
*.log

# ── Virtual envs
qlora-env/
venv/
.venv/

# ── HuggingFace / LangChain caches
hf_cache/
transformers_cache/
cache/
datasets/

# ── Large outputs / checkpoints
output/
GPU_codingassistant/
*.pt
*.bin
*.ckpt
*.safetensors

# ── Docker junk & compose files (we keep these in Git)
Dockerfile*
.dockerignore
docker-compose*.yml
.docker/

# ── IDE / Frontend
.vscode/
node_modules/
dist/
build/
